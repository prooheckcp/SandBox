#include <stdio.h>

int main() {
    int inputNumber = 0;

    int total;
    printf("My number: %d", total);

    return 0;

    printf("Input your favorite number: ");
    scanf("%d", &inputNumber);

    printf("My number is: %d", inputNumber);

    return 0;
}